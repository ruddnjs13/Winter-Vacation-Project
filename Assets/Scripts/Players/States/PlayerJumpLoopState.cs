using Animations;
using Entities;

namespace Players.States
{
    public class PlayerJumpLoopState : EntityState
    {
        public PlayerJumpLoopState(Entity entity, AnimParamSO animParam) : base(entity, animParam)
        {
        }
    }
}